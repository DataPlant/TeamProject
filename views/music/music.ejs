<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/musicStyle.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vollkorn+SC:wght@400;600;700;900&display=swap" rel="stylesheet">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/navbar.ejs') %>
    <h1 id='title'>Your Favorite Music</h1>
    <br>
    <h2 id='add'><a href='/music/newMusic'>Add Song</a></h2>
    <br>
    <ul id='music'>
        <% for(let i=0; i<allMusic.length; i++){ %>
            <br>
            <a href='/music/<%= allMusic[i]._id %>'>
                <h2><%= allMusic[i].title %></h2>
            </a>
            <h3><%= allMusic[i].genre %></h3>
            <br>
            <a href='/music/<%= allMusic[i]._id %>/edit'>Edit Info</a>
            <form action='/music/<%= allMusic[i]._id %>?_method=DELETE' method='POST'>
                <input type='submit' value='Delete Music'/>
            </form>
        <% } %>
    </ul>
</body>
</html>