<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/navbar.ejs') %>
    <ul>
        <h1>Genre:</h1>
        <li><%=genre.genre%></li>
    </ul>
    <h2>Games</h2>
    <% for (let i = 0; i < genre.games.length; i++) { %>
        <a href="/game/<%= genre.games[i]._id %>">
            <li><%= genre.games[i].title %></li>
        </a>
    <% } %>
    <br>
    <br>
    <h2>Movies</h2>
    <% for (let i = 0; i < genre.movies.length; i++) { %>
        <a href="/movie/<%= genre.movies[i]._id %>">
            <li><%= genre.movies[i].title %></li>
        </a>
    <% } %>
    <br>
    <br>
</body>
</html>